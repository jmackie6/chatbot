<Page
    x:Class="CalendarBot.loginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CalendarBot"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <!--Title-->
            <TextBlock Text="Login Page" Foreground="Black" FontSize="40"/>

            <!--UserName-->
            <TextBlock Text="User Name" Foreground="Black" FontSize="30"/>
            <TextBox  BorderBrush="LightGray" Name="UserName" />

            <!--Password-->
            <TextBlock  Foreground="Black" Text="Password" Margin="9,-7,0,0" FontSize="30"/>
            <PasswordBox BorderBrush="LightGray" Name="PassWord" />

            <!--Login Button-->
            <Button Content="Login" Background="Blue" Name="Login" Click="Login_Click" />

            <!--  Registration Button-->
            <Button Content="Sign Up" Background="Blue" Name="SignUp" Click="SignUp_Click"/>

        </StackPanel>
        <Frame Name="LoginFrame" MinHeight="300">

        </Frame>
    </Grid>
</Page>
